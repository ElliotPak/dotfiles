[user]
	email = elliot@elliotpak.com
        name = Elliot Pak
[alias]
        pushall = !git remote | xargs -L1 git push --all
[core]
	excludesfile = ~/.gitignore
[push]
	default = upstream
[credential]
	helper = cache --timeout=1800
